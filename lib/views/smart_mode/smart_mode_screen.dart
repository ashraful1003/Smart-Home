import 'package:flutter/material.dart';

class SmartModeScreen extends StatelessWidget {
  const SmartModeScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
